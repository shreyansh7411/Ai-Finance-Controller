spring:
  application:
    name: ai-fin-controller

  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/ai-fin-controller}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:}
    driver-class-name: org.postgresql.Driver
    hikari:
      connection-timeout: 5000
      maximum-pool-size: 5

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  flyway:
    enabled: true
    locations: classpath:db/migration

server:
  port: ${SERVER_PORT:8080}

cors:
  allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:5173,http://localhost:3000}

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: when-authorized

logging:
  level:
    com.aifincontroller: INFO
    org.springframework.web: WARN

razorpay:
  enabled: ${RAZORPAY_ENABLED:false}
  base-url: ${RAZORPAY_BASE_URL:https://api.razorpay.com/v1}
  key-id: ${RAZORPAY_KEY_ID:}
  key-secret: ${RAZORPAY_KEY_SECRET:}

ai:
  provider: ${AI_PROVIDER:gemini}

gemini:
  enabled: ${GEMINI_ENABLED:true}
  base-url: ${GEMINI_BASE_URL:https://generativelanguage.googleapis.com}
  api-key: ${GEMINI_API_KEY:}
  model: ${GEMINI_MODEL:v1beta/models/gemini-3.6-flash}
